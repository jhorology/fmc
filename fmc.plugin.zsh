# ============================================================================
# fmc - zsh plugin entrypoint
# Natural language to zsh command translator using on-device models
# ============================================================================

0=${(%):-%N}
source "${0:A:h}/fmc.zsh"
